# 因子库诊断与扩充方案

## 一、当前因子库概况

**总因子数：** 473个（来自scheme_d数据文件）

### 因子类型分布
| 类别 | 数量 | 状态 | 说明 |
|------|------|------|------|
| 补充动量/量价 | 70 | 有效 | sup_* 前缀 |
| 宏观因子 | 68 | 有效 | macro_* 前缀 |
| 技术指标 | 61 | 有效 | technical_* 前缀 |
| 动量因子 | 51 | 有效 | momentum_* 前缀 |
| Alpha101 | 50 | 有效 | alpha101_* 前缀 |
| 波动率 | 26 | 有效 | volatility_* 前缀 |
| 聚宽动量 | 20 | 有效 | jq_* 前缀 |
| 流动性 | 16 | 有效 | liquidity_* 前缀 |
| 质量因子 | 14 | **7有效+7占位** | quality_* 前缀 |
| 滚动统计 | 12 | 有效 | roll_* 前缀 |
| Barra风险因子 | 10 | 有效 | barra_* 前缀 |
| 估值因子 | 9 | **全部占位(0.0)** | value_placeholder_* |
| 复合衍生 | 8 | 有效 | deriv_* 前缀 |
| 行业相对 | 7 | 有效 | industry_relative_* |
| 成长因子 | 6 | **1有效+5占位** | growth_* 前缀 |
| AQR因子 | 6 | 有效 | aqr_* 前缀 |
| 规模因子 | 5 | **全部占位(NaN)** | size_placeholder_* |
| 股息因子 | 5 | **全部占位(0.0)** | dividend_placeholder_* |
| 情绪因子 | 5 | 有效 | sentiment_* 前缀 |
| Fama-French | 5 | 有效 | ff_* 前缀 |
| 盈利质量 | 4 | 有效 | nm_* 前缀 |
| 流动性冲击 | 3 | 有效 | ps_* 前缀 |

**有效因子：** 约390个  
**占位因子：** 约36个（value×9 + size×5 + dividend×5 + quality_fin×7 + growth_fin×5 + 其他5）

## 二、缺失类别分析

### 1. 价值类因子（完全缺失）
- **现状：** 9个value_placeholder_*全为0.0
- **缺失：** PE、PB、PS、PCF、股息率等经典估值指标
- **数据源：** Tushare `fina_indicator` API已提供pe/pb/ps/pcf字段

### 2. 质量类因子（部分实现）
- **已有：** ROE、ROA、毛利率、资产负债率、流动比率、速动比率、净利润增长率
- **缺失：** 资产周转率、利息覆盖率、现金流比率、应收账款周转率等
- **数据源：** Tushare `fina_indicator` API已提供inv_turn/ar_turn/ocf_to_profit等字段

### 3. 成长类因子（严重不足）
- **仅有：** 净利润同比增长率(netprofit_yoy)
- **缺失：** 营收增长率、EPS增长率、ROE趋势、资本支出增长率等
- **数据源：** Tushare `fina_indicator` API已提供or_yoy/eps等字段

### 4. 股息类因子（完全缺失）
- **现状：** 5个dividend_placeholder_*全为0.0
- **数据源：** Tushare `fina_indicator` API已提供dv_ratio（股息支付率）

### 5. 规模类因子（完全缺失）
- **现状：** 5个size_placeholder_*全为NaN
- **数据源：** Tushare `daily_basic` API已提供total_mv（总市值）

### 6. 反转类因子（完全缺失）
- **现状：** 无1个月/3个月反转因子
- **数据源：** 可从现有momentum因子直接派生（取负值）

### 7. 低波动率因子（部分覆盖）
- **已有：** 历史波动率(volatility_hist_*)
- **缺失：** 波动率比率、低波动率异象因子
- **数据源：** 可从现有波动率因子计算

## 三、数据源与实现方案

### 现有数据获取接口（`data_fetcher.py`）

#### 1. 财务数据接口（已实现）
```python
def get_financial_data(self, ts_code: str, period: str) -> dict:
    """获取财务数据（ROE/净利润增长/毛利率等）"""
    fields = (
        "ts_code,period,roe,netprofit_yoy,"
        "grossprofit_margin,debt_to_assets,"
        "current_ratio,quick_ratio"
    )
```

#### 2. 行情数据接口（已实现）
```python
def get_monthly_basic(self, trade_date: str) -> pd.DataFrame:
    """获取月末基础行情"""
    fields = "ts_code,trade_date,close,total_mv,turnover_rate,volume_ratio"
```

### 可扩展的财务数据字段（同一API调用）

Tushare `fina_indicator` API支持以下字段，只需扩展`get_financial_data()`的fields参数：

| 字段名 | 中文名 | 因子类别 |
|--------|--------|----------|
| pe | 市盈率 | 价值类 |
| pb | 市净率 | 价值类 |
| ps | 市销率 | 价值类 |
| pcf | 市现率 | 价值类 |
| or_yoy | 营收同比增长率 | 成长类 |
| eps | 每股收益 | 成长类 |
| netprofit_q_yoy | 单季度净利润同比 | 成长类 |
| roe_dt | 净资产收益率(扣非) | 质量类 |
| roe_waa | 加权平均净资产收益率 | 质量类 |
| inv_turn | 存货周转率 | 质量类 |
| ar_turn | 应收账款周转率 | 质量类 |
| ocf_to_or | 经营现金流/营业收入 | 质量类 |
| ocf_to_profit | 经营现金流/净利润 | 质量类 |
| debt_eqt_ratio | 产权比率 | 质量类 |
| turnover_days | 营业周期 | 质量类 |
| dv_ratio | 股息支付率 | 股息类 |

## 四、优先开发建议（10个新因子）

### 1. 价值类因子（低难度，直接获取）
| 因子名 | 计算公式 | 数据源 | 预期IC方向 |
|--------|----------|--------|-----------|
| value_pe_ratio | 直接使用fina_indicator.pe | Tushare fina_indicator | 负（低PE好） |
| value_pb_ratio | 直接使用fina_indicator.pb | Tushare fina_indicator | 负（低PB好） |
| value_ps_ratio | 直接使用fina_indicator.ps | Tushare fina_indicator | 负（低PS好） |
| value_pcf_ratio | 直接使用fina_indicator.pcf | Tushare fina_indicator | 负（低PCF好） |

### 2. 成长类因子（低难度，直接获取）
| 因子名 | 计算公式 | 数据源 | 预期IC方向 |
|--------|----------|--------|-----------|
| growth_revenue_yoy | 直接使用fina_indicator.or_yoy | Tushare fina_indicator | 正（成长溢价） |
| growth_eps_yoy | eps / 去年同期eps | Tushare fina_indicator | 正 |

### 3. 质量类因子（低难度，直接获取）
| 因子名 | 计算公式 | 数据源 | 预期IC方向 |
|--------|----------|--------|-----------|
| quality_ocf_ratio | 直接使用fina_indicator.ocf_to_profit | Tushare fina_indicator | 正（盈余质量） |
| quality_inv_turnover | 直接使用fina_indicator.inv_turn | Tushare fina_indicator | 正 |

### 4. 股息类因子（低难度，直接获取）
| 因子名 | 计算公式 | 数据源 | 预期IC方向 |
|--------|----------|--------|-----------|
| dividend_yield | 直接使用fina_indicator.dv_ratio | Tushare fina_indicator | 正（高股息） |

### 5. 规模类因子（低难度，计算）
| 因子名 | 计算公式 | 数据源 | 预期IC方向 |
|--------|----------|--------|-----------|
| size_log_mcap | log(total_mv) | Tushare daily_basic | 负（小市值溢价） |

### 6. 反转类因子（零成本，派生）
| 因子名 | 计算公式 | 数据源 | 预期IC方向 |
|--------|----------|--------|-----------|
| reversal_1m | -momentum_return_20d | 现有momentum因子 | 正（1月反转效应） |

## 五、实施步骤

### 步骤1：扩展数据获取
修改`data_fetcher.py`的`get_financial_data()`方法，增加所需字段：
```python
fields = (
    "ts_code,period,roe,netprofit_yoy,grossprofit_margin,debt_to_assets,"
    "current_ratio,quick_ratio,pe,pb,ps,pcf,or_yoy,eps,netprofit_q_yoy,"
    "roe_dt,roe_waa,inv_turn,ar_turn,ocf_to_or,ocf_to_profit,"
    "debt_eqt_ratio,turnover_days,dv_ratio"
)
```

### 步骤2：实现因子计算
修改`factor_calculator.py`中的对应方法：
1. `_calc_value()`：实现PE/PB/PS/PCF计算
2. `_calc_growth()`：实现营收增长、EPS增长计算
3. `_calc_quality()`：实现周转率、现金流比率计算
4. `_calc_dividend()`：实现股息率计算
5. `_calc_size()`：实现市值对数计算
6. 新增`_calc_reversal()`：实现反转因子计算

### 步骤3：更新因子名称
将占位符替换为实际因子名：
- `value_placeholder_*` → `value_pe_ratio`, `value_pb_ratio`, `value_ps_ratio`, `value_pcf_ratio`
- `size_placeholder_*` → `size_log_mcap`, `size_mcap_rank`, `size_mcap_decile`
- `dividend_placeholder_*` → `dividend_yield`, `dividend_payout_ratio`
- `growth_fin_*` → `growth_revenue_yoy`, `growth_eps_yoy`, `growth_roe_trend`
- `quality_fin_*` → `quality_inv_turnover`, `quality_ar_turnover`, `quality_ocf_ratio`

## 六、预期收益

1. **因子库多样性提升**：新增价值、成长、股息、规模、反转等类别
2. **IC均值提升**：当前仅0.0032，加入基本面因子后有望提升至0.005+
3. **因子相关性降低**：减少动量类因子的同质化
4. **模型稳定性增强**：多因子类别组合降低过拟合风险
5. **数据利用率提升**：充分利用已有Tushare数据，无需新增API调用

## 七、风险与注意事项

1. **数据质量**：Tushare财务数据存在缺失和延迟
2. **因子有效性**：需回测验证新因子在A股市场的有效性
3. **计算复杂度**：新增因子可能增加M0计算时间
4. **中性化处理**：新因子需适配现有4套中性化方案
5. **因子冗余**：避免与现有因子高度相关（>0.8）

**建议先实现前5个价值类因子（PE/PB/PS/PCF/股息率）进行小规模回测，验证有效性后再批量开发。**

---

## 八、新增因子：MISV_FY1 错误定价因子

> **版本**: 1.0  
> **数据源**: 开源免费方案（AKShare / Tushare免费版 / Yahoo Finance）  
> **适用市场**: A股 / 港股 / 美股

### 8.1 因子定义

#### 核心思想
**MISV_FY1** 基于**一阶段剩余价值增长模型**，通过横截面回归估计股票的合理估值，并计算与当前市净率（PB）的偏离程度：

- **MISV_FY1 > 0**: 合理估值 > 当前估值 → **低估**（看涨信号）
- **MISV_FY1 < 0**: 合理估值 < 当前估值 → **高估**（看跌信号）

#### 数学公式

**Step 1: 横截面回归（每期对所有股票）**

$\frac{V_{it}}{B_{it}} = \gamma_0 + \gamma_1 \frac{NI_{it+1}}{B_{it}} + u_{it}$

**Step 2: 计算错误定价因子**

$MISV\_FY1_{it} = \frac{\hat{V}_{it}}{B_{it}} / PB_{it} - 1$

其中 $\frac{\hat{V}_{it}}{B_{it}} = \gamma_0 + \gamma_1 \frac{NI_{it+1}}{B_{it}}$

#### 变量说明

| 符号 | 含义 | 计算方式 |
|:---|:---|:---|
| $V_{it}$ | 总市值 | 收盘价 × 总股本 |
| $B_{it}$ | 净资产（账面价值） | 最新财报：归属于母公司股东的权益 |
| $NI_{it+1}$ | 分析师一致预期FY1归母净利润 | 未来1个完整财年的预测净利润 |
| $PB_{it}$ | 当前市净率 | 总市值 / 净资产 |

### 8.2 开源数据源方案

#### 数据源对比

| 数据项 | 推荐方案 | 备用方案 | 备注 |
|:---|:---|:---|:---|
| **股价/市值** | AKShare | Yahoo Finance | AKShare支持A股实时数据 |
| **财务报表** | AKShare | Tushare | 资产负债表数据 |
| **分析师预期** | AKShare | 手动计算/替代方案 | A股覆盖有限，可用历史ROE替代 |
| **总股本** | AKShare | Tushare | - |

### 8.3 代码实现（已完成）

#### data_fetcher.py 新增方法
```python
def get_analyst_forecast(self, ts_code: str, period: str) -> dict:
    """获取分析师一致预期数据（净利润预测FY1/FY2）"""
    df = self.pro.ana_forecast(
        ts_code=ts_code, period=period,
        fields="ts_code,period,net_profit_fy1,net_profit_fy2,pe_fy1,pe_fy2"
    )
    return df.iloc[0].to_dict() if not df.empty else {}

def get_daily_valuation(self, trade_date: str) -> pd.DataFrame:
    """获取全市场每日估值指标（PB/PE/PS等）"""
    df = self.pro.daily_basic(
        trade_date=trade_date,
        fields="ts_code,trade_date,close,total_mv,circ_mv,pe,pb,ps,pcf"
    )
    return df

def get_balance_sheet(self, ts_code: str, period: str) -> dict:
    """获取资产负债表数据（净资产等）"""
    df = self.pro.balancesheet(
        ts_code=ts_code, period=period,
        fields="ts_code,period,total_hldr_eqy_exc_min_int,total_assets,total_liab"
    )
    return df.iloc[0].to_dict() if not df.empty else {}
```

#### factor_calculator.py 新增方法
```python
def calc_misv_fy1(self, pb: float, net_equity: float, ni_fy1: float, 
                  gamma_0: float, gamma_1: float) -> float:
    """
    错误定价因子 MISV_FY1
    公式: MISV_FY1 = (V_hat / B) / PB - 1
    其中: V_hat / B = gamma_0 + gamma_1 * (NI_FY1 / B)
    """
    ni_over_b = ni_fy1 / net_equity
    v_hat_over_b = gamma_0 + gamma_1 * ni_over_b
    return v_hat_over_b / pb - 1

@staticmethod
def cross_sectional_regression(pb_series, net_equity_series, ni_fy1_series) -> tuple:
    """横截面回归：V/B = gamma_0 + gamma_1 * (NI_FY1 / B) + u"""
    # 简单线性回归实现
    y = pb_series.values  # 因变量: V/B = PB
    x = (ni_fy1_series / net_equity_series).values  # 自变量: NI_FY1 / B
    # OLS回归计算 gamma_0, gamma_1
    ...
    return gamma_0, gamma_1
```

### 8.4 使用流程

由于 MISV_FY1 需要横截面回归，需在 **pipeline 层面** 调用：

```python
# 1. 获取全市场数据
fetcher = TushareFetcher()
valuation_df = fetcher.get_daily_valuation(trade_date)  # PB数据
# ... 获取所有股票的净资产和分析师预期

# 2. 横截面回归
gamma_0, gamma_1 = FactorCalculator.cross_sectional_regression(
    pb_series, net_equity_series, ni_fy1_series
)

# 3. 计算每只股票的MISV_FY1
calculator = FactorCalculator()
misv_fy1 = calculator.calc_misv_fy1(pb, net_equity, ni_fy1, gamma_0, gamma_1)
```

### 8.5 因子特性

| 属性 | 说明 |
|:---|:---|
| **因子类别** | 价值类 / 错误定价类 |
| **预期IC方向** | 正（低估股票未来收益更高） |
| **计算频率** | 月度（每月末计算） |
| **数据依赖** | PB、净资产、分析师预期净利润 |
| **特殊处理** | 需横截面回归，不能单股独立计算 |