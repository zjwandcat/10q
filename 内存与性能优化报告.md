# 三项内存与性能优化报告

## 优化1：bottleneck 替换 numpy 统计函数

| 项目 | 值 |
|---|---|
| bottleneck 版本 | **1.6.0** |
| 重要发现 | `bn.mean`/`bn.std` 不存在，改用 `bn.nanmean`/`bn.nanstd`（语义等价，热路径数据不含NaN） |

**各文件替换数：**

| 文件 | `_mean()` | `_std()` | 合计 |
|---|---|---|---|
| `m2_engine/ensemble.py` | 10 | 7 | **17** |
| `m2_engine/run_m2.py` | 3 | 0 | **3** |
| **总计** | 13 | 7 | **20** |

**替换前后对比（ensemble.py 滚动IR计算）：**

```python
# 替换前
std_ex = np.std(w_ex, ddof=0)
ir = np.mean(w_ex) / std_ex * np.sqrt(12)

# 替换后
std_ex = _std(w_ex, ddof=0)
ir = _mean(w_ex) / std_ex * np.sqrt(12)
```

---

## 优化2：Windows 强制内存归还

| 项目 | 值 |
|---|---|
| win_memory.py | 已创建，**1,454 bytes** |
| 核心机制 | `gc.collect()` + `SetProcessWorkingSetSize(-1, -1)` |

**4个插入位置：**

| # | 文件 | 行号 | 上下文 |
|---|---|---|---|
| 1 | `objective.py` | **L235** | `release_memory_to_os()` ← 替换原 `gc.collect()` |
| 2 | `objective.py` | **L264** | `release_memory_to_os()` ← except 块，`return -999.0` 前 |
| 3 | `app.py` | **L2540** | `release_memory_to_os()` ← `_memory_watchdog` 中 `logger.critical(...)` 后 |
| 4a | `app.py` | **L1644** | `release_memory_to_os()` ← `stop_now()` 中 `cancel_graceful_timer()` 后 |
| 4b | `app.py` | **L2437** | `release_memory_to_os()` ← `stop_p2_now()` 中 `cancel_graceful_timer()` 后 |

**"内存归还"日志测试**：需运行5个Trial后观察。当 RSS 释放 > 0.1GB 时会输出：

```
内存归还: 7.2GB → 5.8GB（释放1.4GB）
```

当前环境未运行完整Trial，待下次M5运行时验证。

---

## 优化3：股票代码列转 Categorical

| 项目 | 值 |
|---|---|
| 实际列名 | **`stock_code`** |
| 转换位置 | `data_loader.py` L156-162，`factor_df.copy()` 之后 |

**内存差异（50万行模拟）：**

| dtype | 内存 |
|---|---|
| object | 27.5 MB |
| category | 1.1 MB |
| **节省** | **26.4 MB（96%）** |

**`.isin()` / `groupby` 兼容性确认**（Categorical 与 object 行为完全一致）：

| 调用位置 | 代码 |
|---|---|
| `run_m2.py:331` | `ret_df.groupby("trade_date")` |
| `run_m2.py:355` | `factor_df["trade_date"].isin(keep_months)` |
| `ensemble.py:106` | `train_v.groupby("trade_date").size()` |

> 注：以上 groupby/isin 均作用于 `trade_date` 列（非 stock_code），Categorical 的 `stock_code` 列在 `ensemble.py:252` 处通过 `.values` 取出使用，与 dtype 无关。
