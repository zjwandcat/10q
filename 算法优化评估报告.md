# M5优化器 — 算法层面优化评估报告

> 生成日期：2026-06-04  
> 评估范围：纯 CPU 模式下的算法层优化路径  
> 排除项：所有涉及修改 `*_p1_config.json` 参数的改动

---

## 一、项目架构现状

| 层级 | 当前配置 | 说明 |
|------|----------|------|
| 外层窗口并行 | `GLOBAL_N_JOBS_OUTER = 1` | 强制串行（低内存模式） |
| 内层模型训练 | LGBM 4线程 + XGB 4线程 | LightGBM/XGBoost 内部 C++ 线程池 |
| 模型训练方式 | 双模型串行（LGBM → XGB） | 避免 native 内存崩溃 |
| 搜索空间维度 | 30 个参数 | LGBM(12) + XGB(11) + Ensemble(1) + Feature(5) + Window(1) |
| 目标函数 | MAE回归（regression_l1 / reg:absoluteerror） | 非排名目标（lambdarank 有内存崩溃历史） |

---

## 二、各层问题与优化方案

### 2.1 LabelMaker 层

**文件**：`m1_engine/label_maker.py`

| 问题 | 严重度 | 当前实现 |
|------|--------|----------|
| `groupby.transform(lambda)` 逐组调用 Python 函数 | ⭐⭐⭐⭐ | `transform(lambda x: x.rank(...))` |

**优化**：用 `groupby.rank()` 直接替代 lambda：
```python
# 当前
df.groupby("trade_date")["Target_Return_1M"].transform(lambda x: x.rank(...))
# 优化
df.groupby("trade_date")["Target_Return_1M"].rank(...)
```
**预估收益**：label 制作时间缩减 **30-50%**

---

### 2.2 RollingSplitter 层

**文件**：`m1_engine/rolling_splitter.py`

| 问题 | 严重度 | 说明 |
|------|--------|------|
| `.isin()` 每次 O(N) 全表扫描 | ⭐⭐⭐⭐ | 100万行 × 100窗口 = 3亿次比较 |
| 连续窗口 80% 数据重叠但完全重新拷贝 | ⭐⭐⭐ | 无增量更新机制 |

**优化**：数据预排序后用 `iloc` 区间切片（二分查找定位月份边界），窗口直接 O(1) 切片。

**预估收益**：窗口切片时间缩减 **60-80%**

---

### 2.3 FeatureStore 层

**文件**：`m2_engine/feature_store.py`

#### 2.3.1 截面 Z-score 内存峰值

| 问题 | 严重度 | 说明 |
|------|--------|------|
| `diff_sq = (vals - row_means) ** 2` 分配全量矩阵 | ⭐⭐⭐⭐ | 50万行 × 500因子 × 4B ≈ 1GB 峰值 |

**优化**：in-place 原地更新，避免 `diff_sq` 全量分配。

**预估收益**：内存峰值降低 **50%**，间接提速 **10-15%**

#### 2.3.2 高相关去重

| 问题 | 严重度 | 说明 |
|------|--------|------|
| `max_corr=0.95` 门槛过高 | ⭐⭐⭐ | 保留太多高度相关因子，增加模型训练负担 |
| 相关矩阵用全量数据计算 | ⭐⭐ | 50万行 → 可抽样到 5 万行 |

**优化**：
1. `max_corr` 降到 **0.85-0.90**
2. 相关矩阵用随机抽样计算

**预估收益**：特征数减少 **20-40%** → 模型训练时间减少 **20-30%**

#### 2.3.3 IC 筛选门槛

| 问题 | 严重度 | 说明 |
|------|--------|------|
| `min_ic_abs=0.003` 几乎等于没过滤 | ⭐⭐⭐⭐ | 随机噪声 IC 可达 0.002-0.005 |

**优化**：`min_ic_abs` 提高到 **0.008-0.015**

**预估收益**：弱因子减少 → 模型训练时间减少 **15-25%**

---

### 2.4 模型训练层

**文件**：`m2_engine/lgbm_model.py`、`m2_engine/xgb_model.py`

#### 2.4.1 窗口间没有 Warm Start（最大优化空间）

| 问题 | 严重度 | 说明 |
|------|--------|------|
| 连续窗口 train 数据 35/36 重叠，但完全从零训练 | ⭐⭐⭐⭐⭐ | 每个窗口独立训练，浪费大量时间 |

**优化**：同一 Trial 内，窗口间用 `init_model` 加载上一窗口的 booster：
```python
# LightGBM
model = lgb.train(..., init_model=prev_booster)
# XGBoost  
model = xgb.train(..., xgb_model=prev_model)
```

**预估收益**：模型训练时间减少 **40-60%**

**⚠️ 风险**：需实验验证 IC 是否受 Warm Start 偏差影响

#### 2.4.2 已有优化项（无需改动）

| 优化项 | 状态 |
|--------|------|
| `max_bin=128`（默认 255） | ✅ 已优化 |
| `float32` 全程计算 | ✅ 已优化 |
| `early_stopping` | ✅ 已启用 |
| `_fast_rank_corr`（两次 argsort） | ✅ 已优化 |

---

### 2.5 Ensemble 层

**文件**：`m2_engine/ensemble.py`

#### compute_val_portfolio_metrics 月度循环

| 问题 | 严重度 | 说明 |
|------|--------|------|
| `for date in ...` 逐月迭代（30-50 次） | ⭐⭐ | 每次包含 head/sort/map 等操作 |

**优化**：用 `groupby` 替代 for 循环。

**预估收益**：**5-10%**（此步骤本身耗时占比不高）

---

### 2.6 Optuna 优化层

**文件**：`m5_optimizer/phase1_global.py`、`m5_optimizer/search_space.py`

#### 2.6.1 没有 Pruner

| 问题 | 严重度 | 说明 |
|------|--------|------|
| 差参数会跑完全部窗口才被判死刑 | ⭐⭐⭐⭐⭐ | 每个 Trial 可能耗时 30-60 分钟 |

**优化**：添加 `MedianPruner` 或 `HyperbandPruner`，每完成 N 个窗口向 Optuna report 中间值，提前终止差 Trial。

**预估收益**：**30-50%** 的 Trial 被提前终止 → 总优化时间减少 **30-40%**

#### 2.6.2 搜索空间过大 vs Trial 预算过小

| 问题 | 严重度 | 说明 |
|------|--------|------|
| 30 维搜索空间需要 500-1000 个 Trial 才能收敛 | ⭐⭐⭐⭐⭐ | 当前只跑 50 个 Trial |
| LGBM/XGB 的 lr_mode、decay 等 6 个参数影响有限 | ⭐⭐⭐ | 二阶参数，对最终 IC 影响很小 |

**优化方向**：

| 操作 | 节省参数数 | 说明 |
|------|-----------|------|
| 固定 lr_mode=decay，移除 decay_every/decay_factor 搜索 | -6 | 学习率衰减收益有限，固定即可 |
| 固定 depth_mode=adaptive，移除搜索 | -2 | 自适应深度总是更好 |
| LGBM 和 XGB 共用同一组 tree 参数 | -5 | 两者最优参数高度相关 |
| **预估剩余** | **~17 个** | 收敛 Trial 数从 1000 降到 ~200 |

#### 2.6.3 n_startup_trials 偏少

| 问题 | 严重度 | 说明 |
|------|--------|------|
| `n_startup_trials=15` 对 30 个参数太少 | ⭐⭐⭐⭐ | TPE 需要 `n_params × 1-2` 个随机 Trial |

**优化**：增加到 **40-50**

---

## 三、综合优化优先级排序

| 优先级 | 优化项 | 预估收益 | 实施难度 | 风险 |
|--------|--------|---------|---------|------|
| 🔥🔥🔥 | **窗口间 Warm Start（模型复用）** | 40-60% | 中 | ⚠️ 需验证 IC |
| 🔥🔥🔥 | **添加 Optuna Pruner** | 30-40% | 低 | 低 |
| 🔥🔥🔥 | **减少搜索空间维度（30→17）** | 30-50% | 低 | 中 |
| 🔥🔥 | **预排序 + iloc 窗口切片** | 60-80% 切片时间 | 中 | 低 |
| 🔥🔥 | **提高 min_ic_abs 门槛** | 20-30% | 低 | 中 |
| 🔥🔥 | **降低 max_corr 到 0.85** | 20-30% | 低 | 低 |
| 🔥 | **增加 n_startup_trials 到 40** | 质量提升 | 极低 | 无 |
| 🔥 | **groupby.rank() 替代 lambda** | 30-50% label时间 | 极低 | 无 |
| 🔥 | **Z-score 内存优化** | 50% 内存 | 中 | 低 |
| ⭐ | **向量化 compute_val_portfolio_metrics** | 5-10% | 中 | 低 |

---

## 四、总结

**最大瓶颈不是单步效率，而是系统性设计**：

1. **窗口间没有模型复用** — 每个窗口从零训练是最大的时间浪费
2. **Optuna 没有 Pruner** — 差 Trial 跑完全部窗口才终止
3. **搜索空间 30 维 vs 50 个 Trial** — 优化效率极低，如同大海捞针

这三项优化叠加可带来 **60-80%** 的总时间缩减，且不需要改动核心算法逻辑。
