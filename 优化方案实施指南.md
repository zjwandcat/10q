# M2 量化选股回测系统 — 优化方案实施指南


## 方案1：线程分配 nthread 4→14

**优先级：最高 | 改动量：2行 | 预期提速：1.9\~2.6x**

模型训练是纯CPU计算，原配置只给4线程（`cpu\_count//4`），16核机器浪费了75%算力。

**涉及文件：** `lgbm\_model.py`、`xgb\_model.py`

**改法：**

两个文件顶部，把：

```python
M5\_NTHREAD\_PER\_MODEL = max(\_cores // 4, 2)
```

改为：

```python
def \_get\_optimal\_nthread() -> int:
    """主进程串行训练时独占全部核心，子进程并行时每进程4核"""
    cpu\_count = os.cpu\_count() or 8
    if os.environ.get('JOBLIB\_WORKER\_ID') is not None:
        return max(cpu\_count // 4, 2)  # 子进程：少分线程避免oversubscription
    return max(cpu\_count - 2, 4)       # 主进程：留2核给系统和GC

M5\_NTHREAD\_PER\_MODEL = \_get\_optimal\_nthread()
```

`lgbm\_model.py` 额外一处，fit()内：

```python
"histogram\_pool\_size": 2048
```

改为：

```python
"histogram\_pool\_size": max(2048, cpu\_count \* 256)
```

`lgbm\_model.py` 和 `xgb\_model.py` 的fit()内，nthread赋值改为：

```python
lgb\_params\["nthread"] = \_get\_optimal\_nthread()   # 原来是 M5\_NTHREAD\_PER\_MODEL
xgb\_params\["nthread"] = \_get\_optimal\_nthread()   # 原来是 M5\_NTHREAD\_PER\_MODEL
```

\---

## 方案2：修复 exec(f"del {name}") 反模式

**优先级：高 | 改动量：5行 | 预期提速：无性能提升但是bug修复**

原代码用 `exec(f"del {name}")` 释放变量，这在函数局部作用域中不生效（Python3的exec无法修改函数局部变量），等于没释放。

**涉及文件：** `run\_m2.py`

**改法：**

`\_process\_single\_window()` 的 try 块开头加一行初始化：

```python
train\_p = val\_p = pred\_p = predictor = result = None
```

finally 块中，把：

```python
del train\_df, val\_df, pred\_df
for name in \["train\_p","val\_p","pred\_p","predictor","result"]:
    try:
        exec(f"del {name}")
    except Exception:
        pass
gc.collect()
```

改为：

```python
del train\_df, val\_df, pred\_df
for obj in \[train\_p, val\_p, pred\_p, predictor, result]:
    if obj is not None:
        try:
            del obj
        except Exception:
            pass
gc.collect()
```

\---

## 方案3：FeatureStore Z-score 向量化

**优先级：高 | 改动量：约40行（新增函数+替换调用） | 预期提速：2.1x**

原 `groupby.transform("mean"/"std")` 每列都要走Python层groupby调度，换成NumPy的 `bincount` 纯C实现，跳过Python开销。

**涉及文件：** `feature\_store.py`

**改法：**

新增函数（放在class定义之前）：

```python
def \_zscore\_numpy(df: pd.DataFrame, cols: list) -> pd.DataFrame:
    """NumPy向量化Z-score，替代groupby.transform"""
    if not cols:
        return df

    df\[cols] = df\[cols].astype(np.float32)

    dates = df\["trade\_date"].values
    vals  = df\[cols].values  # (n\_samples, n\_features) float32

    unique\_dates, inverse = np.unique(dates, return\_inverse=True)
    n\_dates = len(unique\_dates)
    n\_features = vals.shape\[1]

    counts = np.bincount(inverse, minlength=n\_dates).astype(np.float32)

    # 分组均值
    means = np.zeros((n\_dates, n\_features), dtype=np.float32)
    for j in range(n\_features):
        means\[:, j] = (np.bincount(inverse, weights=vals\[:, j],
                                    minlength=n\_dates) / counts)
    row\_means = means\[inverse]

    # 分组方差
    diff\_sq = (vals - row\_means) \*\* 2
    variances = np.zeros\_like(means)
    for j in range(n\_features):
        variances\[:, j] = (np.bincount(inverse, weights=diff\_sq\[:, j],
                                        minlength=n\_dates) / counts)
    stds = np.sqrt(variances)
    stds\[stds < 1e-6] = 1.0
    row\_stds = stds\[inverse]

    df\[cols] = ((vals - row\_means) / row\_stds).astype(np.float32)
    return df
```

`FeatureStore.fit\_transform()` 中，把原 `\_zscore()` 函数和调用全部删除，改为：

```python
# 原: train\_p = \_zscore(train\_p, non\_macro)
# 改:
train\_p = \_zscore\_numpy(train\_p, non\_macro)
val\_p   = \_zscore\_numpy(val\_p,   non\_macro)
pred\_p  = \_zscore\_numpy(pred\_p,  non\_macro)
```

\---

## 方案4：进程启动方式设为 spawn

**优先级：中 | 改动量：4行 | 预期提速：无性能提升但防死锁**

Linux/macOS默认fork，fork后LightGBM/XGBoost的C层锁状态会继承导致死锁。Windows默认spawn无需此改动，但加了更安全。

**涉及文件：** `run\_m2.py`

**改法：**

文件顶部 import 区域加：

```python
import multiprocessing

try:
    multiprocessing.set\_start\_method('spawn', force=True)
except RuntimeError:
    pass  # 已设置过
```

\---

## 方案5：joblib 后端 threading→loky（仅16核推荐）

**优先级：中（16核推荐，4核不推荐） | 改动量：约30行 | 预期提速：16核下2\~3x并行加速**

原 `backend="threading"` 受Python GIL限制，CPU密集任务无法真正并行。loky用子进程绕过GIL，但子进程有启动开销（4核实测反而更慢，16核才有收益）。

⚠️ **注意：此方案需删除全局缓存，且每窗口内存会翻倍（子进程独立内存），16GB内存建议最多2窗口并行。**

**涉及文件：** `run\_m2.py`

**改法：**

1. 删除全局缓存相关代码（约20行）：

```python
# 删除以下全部：
\_FEATURE\_CACHE: Dict = {}
\_FEATURE\_CACHE\_MAX\_SIZE = 0
\_FEATURE\_CACHE\_LOCK = threading.Lock()
\_FEATURE\_CACHE\_PARAMS\_HASH: Optional\[int] = None
```

2. `\_process\_single\_window()` 中删除缓存读写逻辑（`global \_FEATURE\_CACHE` 那一段），改为每次直接调FeatureStore。
3. `run\_m2()` 中删除缓存管理代码：

```python
# 删除：
global \_FEATURE\_CACHE, \_FEATURE\_CACHE\_LOCK, \_FEATURE\_CACHE\_MAX\_SIZE, \_FEATURE\_CACHE\_PARAMS\_HASH
# 删除：
if fast\_mode and fast\_window\_count <= 20:
    \_FEATURE\_CACHE\_MAX\_SIZE = fast\_window\_count
else:
    \_FEATURE\_CACHE\_MAX\_SIZE = 0
with \_FEATURE\_CACHE\_LOCK:
    \_FEATURE\_CACHE.clear()
# 删除：
new\_hash = ...
if new\_hash != \_FEATURE\_CACHE\_PARAMS\_HASH:
    with \_FEATURE\_CACHE\_LOCK:
        \_FEATURE\_CACHE.clear()
        \_FEATURE\_CACHE\_PARAMS\_HASH = new\_hash
```

4. `\_safe\_parallel\_jobs()` 中，上限3→2（16GB内存保守值）：

```python
def \_safe\_parallel\_jobs():
    available\_gb = psutil.virtual\_memory().available / 1024\*\*3
    by\_memory = max(int(available\_gb / 0.8), 1)   # 每窗口约0.8GB
    by\_cpu = max(os.cpu\_count() // 4, 1)
    return min(by\_memory, by\_cpu, 2)               # 16GB内存上限2窗口
```

5. joblib调用改为loky：

```python
batch\_results = Parallel(
    n\_jobs=min(n\_jobs, len(windows\_list)),
    backend="loky",
    max\_nbytes='256M',    # 大于256M的数据用文件传递，减少内存拷贝
    verbose=0,
)(
    delayed(\_process\_single\_window)(...)
    ...
)
```

6. 可删除 `import threading`（如果不再需要）。

\---

## 方案6：EnsemblePredictor 内存优化

**优先级：低 | 改动量：3行 | 预期提速：微小，但减少内存碎片**

**涉及文件：** `ensemble.py`

**改法：**

1. 类定义加 `\_\_slots\_\_`：

```python
class EnsemblePredictor:
    \_\_slots\_\_ = \[
        'lgbm\_weight', 'xgb\_weight', 'lgbm', 'xgb', '\_gpu\_cfg',
        'IC\_PENALIZE\_THRESHOLD', 'IC\_WARN\_THRESHOLD',
    ]
```

2. `fit\_predict()` 中模型训练完成后加一行：

```python
# 在 self.xgb.fit(...) 之后、验证集预测之前：
del X\_train, y\_train, X\_val, y\_val, g\_train, g\_val
```

\---

## 方案7：数据加载优化（有Polars才改）

**优先级：低 | 改动量：约20行 | 预期提速：I/O阶段2\~3x**

仅当环境已安装Polars时启用，否则跳过。

**涉及文件：** `smart\_preprocessor.py`

**改法：**

1. 顶部加Polars检测：

```python
try:
    import polars as pl
    HAS\_POLARS = True
except ImportError:
    HAS\_POLARS = False
```

2. `\_read\_parquet\_safe()` 改为优先Polars：

```python
def \_read\_parquet\_safe(path) -> pd.DataFrame:
    try:
        if HAS\_POLARS:
            df = pl.scan\_parquet(str(path)).collect().to\_pandas()
        else:
            df = pd.read\_parquet(path)
        return df
    except Exception as e:
        logger.warning(f"读取失败: {path.name} - {e}")
        return None
```

3. `\_optimize\_dataframe()` 逐列循环改批量：

```python
# 原：for col in df.columns: if df\[col].dtype == "float64": df\[col] = ...
# 改：
float64\_cols = \[c for c in df.columns if c not in meta\_cols and df\[c].dtype == "float64"]
int64\_cols   = \[c for c in df.columns if c not in meta\_cols and df\[c].dtype == "int64"]
if float64\_cols:
    df\[float64\_cols] = df\[float64\_cols].astype(np.float32)
if int64\_cols:
    df\[int64\_cols] = df\[int64\_cols].astype(np.int32)
```